"use strict";
function UpdateUser(updatedata) {
}
UpdateUser({ name: 'santosh', age: 34 });
const user = { name: 'santosh', age: 3 };
user.name = 'harjlsjf';
const handleEvent = (event) => {
    console.log(`Handling event: ${event}`);
};
handleEvent('click'); // OK
